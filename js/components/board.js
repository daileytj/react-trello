import React from 'react';
import List from './list';

export default function Board() {
    return ( <
        div class = "board" >
        <List / >
        <List / >
        <List / >
        </div>
    );
}
